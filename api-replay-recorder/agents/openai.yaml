interface:
  display_name: "Fake Human UI Recorder"
  short_description: "Record browser flows and preflight API replay"
  default_prompt: "Use $api-replay-recorder to record a human website operation once, preserve UI and network evidence, classify replay feasibility, preflight the API recipe, and only finalize materials after accepted API replay."
